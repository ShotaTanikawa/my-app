services:
  postgres:
    image: postgres:16
    container_name: order-mgmt-postgres
    environment:
      POSTGRES_DB: order_mgmt
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
